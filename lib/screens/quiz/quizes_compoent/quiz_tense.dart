import 'package:flutter/material.dart';

class QuizTense extends StatelessWidget {
  const QuizTense({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
        child: Text('Quiz 3: Choose the correct tense of the word'));
  }
}
